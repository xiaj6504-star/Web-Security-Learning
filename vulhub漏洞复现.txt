####kali
cat /etc/os-release && uname -a  查看系统版本和Linux内核版本

####docker安装
1、验证是否安装          docker --version 
2、卸载旧版（如果有） sudo apt remove docker docker-engine docker.io containerd runc -y    
3、安装依赖工具          sudo apt update
                             sudo apt install -y ca-certificates curl gnupg lsb-release 
4、添加 Docker 官方 GPG 密钥    
                             sudo mkdir -p /etc/apt/keyrings
                             curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
                             sudo chmod a+r /etc/apt/keyrings/docker.gpg
5、添加 Docker APT 源（关键：使用 Debian 12 "bookworm" 的源）
                             echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian bookworm stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
6、更新 APT 并安装 Docker
                             sudo apt update
                             sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
7、将当前用户加入 docker 组（避免每次用 sudo）
                             sudo usermod -aG docker $USER                     //把当前用户（kali）加入到 docker 用户组里
                             newgrp docker                                                 //刷新组权限
                             groups                                                             //组权限里面包含docker
8、验证版本号
                             docker --version
                             docker compose version

####vulhub源码                                                                           
           //它本质上是一个开源项目，所有环境配置文件都托管在 GitHub 上。官网（vulhub.org）主要提供项目介绍和文档，并不提供直接下载
           // Vulhub：是一个开源项目，提供的是基于 Docker 的漏洞环境配置文件（如 docker-compose.yml），所以通过 git clone 获取代码